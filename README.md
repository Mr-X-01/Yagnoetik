# Yagnoetik VPN - Необнаруживаемый VPN-сервис

Полностью кастомный VPN-сервис, разработанный для работы в условиях жёсткой цензуры. Маскируется под обычный российский SaaS-бэкенд.

## 🚀 Быстрое развертывание на сервере

### Автоматическая установка на Ubuntu 24.04

Для развертывания на production сервере с настоящим доменом и SSL сертификатами:

```bash
# На вашем Ubuntu 24.04 сервере
wget https://raw.githubusercontent.com/Mr-X-01/Yagnoetik/main/deploy/install.sh
chmod +x install.sh
sudo ./install.sh your-domain.com admin@your-domain.com
```

**Готово!** Скрипт автоматически:
- ✅ Клонирует репозиторий с GitHub
- ✅ Установит все зависимости (Go, Nginx, Certbot)
- ✅ Получит Let's Encrypt SSL сертификаты
- ✅ Настроит Nginx с HTTPS и gRPC поддержкой
- ✅ Создаст systemd сервисы с автозапуском
- ✅ Сгенерирует protobuf файлы
- ✅ Соберет и запустит все компоненты

### После установки доступно:
- **VPN сервер**: `https://your-domain.com`
- **Админ-панель**: `https://your-domain.com:8080`
- **Управление**: команды `yagnoetik start/stop/restart/status/logs`

## Архитектура

### Production сервер (за Nginx)
- **Порт 443**: HTTPS + gRPC (основной трафик) → 8444
- **Порт 8080**: Админ-панель HTTPS → 8081
- **Внутренний порт 8443**: REST API для управления клиентами
- **Протокол**: gRPC handshake → кастомный XChaCha20-Poly1305
- **Маскировка**: Легитимный JSON API + валидные gRPC-фреймы

### Клиенты
- **Windows**: Нативный GUI + TUN интерфейс
- **Android**: Go Mobile библиотека + VpnService

### Админ-панель
- **Веб-интерфейс**: HTMX + Alpine.js
- **Функции**: Создание/блокировка клиентов, статистика

## 🛠️ Локальная разработка

### 1. Клонирование репозитория

```bash
git clone https://github.com/Mr-X-01/Yagnoetik.git
cd Yagnoetik
```

### 2. Генерация protobuf файлов

```bash
# Установка protoc и плагинов
go install google.golang.org/protobuf/cmd/protoc-gen-go@latest
go install google.golang.org/grpc/cmd/protoc-gen-go-grpc@latest

# Генерация для сервера
cd server
protoc --go_out=. --go_opt=paths=source_relative \
    --go-grpc_out=. --go-grpc_opt=paths=source_relative \
    proto/tunnel.proto

# Генерация для Windows клиента
cd ../client-windows
protoc --go_out=. --go_opt=paths=source_relative \
    --go-grpc_out=. --go-grpc_opt=paths=source_relative \
    proto/tunnel.proto
```

### 3. Сборка компонентов

```bash
# Сервер
cd server
go mod tidy
go build -o yagnoetik-server ./cmd/server

# Windows клиент
cd ../client-windows
go mod tidy
go build -o yagnoetik-client.exe ./cmd

# Android клиент
cd ../client-android
go mod tidy
gomobile bind -target=android .

# Админ-панель
cd ../admin-panel
go mod tidy
go build -o yagnoetik-admin .
```

### 4. Локальное тестирование

```bash
# Генерация тестовых сертификатов
cd server
go run generate_certs.go

# Запуск сервера
API_KEY="test-api-key" ./yagnoetik-server

# Запуск админ-панели (в другом терминале)
cd ../admin-panel
SERVER_URL="http://localhost:8443" API_KEY="test-api-key" ./yagnoetik-admin
```

## Конфигурация

### Сервер

Отредактируйте `server/cmd/server/main.go`:
- Измените API ключ (строка 45)
- Укажите пути к сертификатам

### Клиенты

Создайте `config.json`:
```json
{
  "server_addr": "your-server.com",
  "uuid": "client-uuid-from-admin",
  "secret": "client-secret-from-admin",
  "key": [32 байта ключа в base64]
}
```

## 🔧 Управление production сервером

### Команды управления

После автоматической установки доступны команды:

```bash
yagnoetik start      # Запуск всех сервисов
yagnoetik stop       # Остановка всех сервисов
yagnoetik restart    # Перезапуск всех сервисов
yagnoetik status     # Статус сервисов
yagnoetik logs       # Просмотр логов в реальном времени
yagnoetik build      # Пересборка проекта после обновления
```

### Обновление системы

```bash
# Остановить сервисы
yagnoetik stop

# Обновить код из GitHub
cd /opt/yagnoetik/Yagnoetik
git pull

# Пересобрать и запустить
yagnoetik build
yagnoetik start
```

### Мониторинг

```bash
# Логи всех сервисов
yagnoetik logs

# Логи конкретного сервиса
journalctl -f -u yagnoetik-server
journalctl -f -u yagnoetik-admin

# Статус системы
systemctl status yagnoetik-server yagnoetik-admin nginx

# Проверка портов
ss -tulpn | grep -E ':(443|8080|8444|8443|8081)'
```

## API управления клиентами

### Через веб-интерфейс
Откройте админ-панель: `https://your-domain.com:8080`

### Через API
```bash
# Создание клиента
curl -X POST http://localhost:8443/api/clients \
  -H "X-API-Key: your-api-key" \
  -H "Content-Type: application/json" \
  -d '{"duration": "30d"}'

# Список клиентов
curl http://localhost:8443/api/clients \
  -H "X-API-Key: your-api-key"

# Блокировка клиента
curl -X POST http://localhost:8443/api/clients/{uuid}/block \
  -H "X-API-Key: your-api-key"

# Разблокировка клиента
curl -X POST http://localhost:8443/api/clients/{uuid}/unblock \
  -H "X-API-Key: your-api-key"

# Удаление клиента
curl -X DELETE http://localhost:8443/api/clients/{uuid} \
  -H "X-API-Key: your-api-key"
```

## Безопасность

### Особенности маскировки
- Первые 128-256 байт — валидные gRPC фреймы
- JA3 fingerprint идентичен Go http.Client
- Легитимные HTTP эндпоинты отвечают реальными данными
- Энтропия трафика максимально близка к случайной

### Рекомендации
- Используйте только российские домены и хостинг
- Регулярно обновляйте SSL сертификаты
- Мониторьте логи на предмет подозрительной активности
- Ограничивайте количество клиентов на сервер

## Мониторинг

### Логи сервера
```bash
journalctl -u yagnoetik -f
```

### Проверка работоспособности
```bash
# Проверка основного сервера
curl https://your-domain.com/health

# Проверка маскировочных эндпоинтов
curl https://your-domain.com/api/v1/status
curl https://your-domain.com/api/v1/info

# Проверка админ-панели
curl -I https://your-domain.com:8080
```

## Устранение неполадок

### Сервер не запускается
- Проверьте права доступа к сертификатам
- Убедитесь, что порты 443 и 8443 свободны
- Проверьте логи: `journalctl -u yagnoetik`

### Клиент не подключается
- Проверьте правильность UUID и secret
- Убедитесь в доступности сервера
- Проверьте настройки файрвола

### Низкая скорость
- Проверьте загрузку CPU (шифрование)
- Оптимизируйте MTU размер
- Рассмотрите использование нескольких серверов

## Структура проекта

```
yagnoetik/
├── server/                 # Серверная часть
│   ├── cmd/server/        # Точка входа
│   ├── internal/          # Внутренняя логика
│   │   ├── api/          # REST API и cover endpoints
│   │   ├── auth/         # Управление клиентами
│   │   ├── crypto/       # Шифрование
│   │   ├── protocol/     # Кастомный протокол
│   │   └── tunnel/       # gRPC туннель
│   └── proto/            # Protobuf определения
├── client-windows/        # Windows клиент
│   ├── cmd/              # GUI приложение
│   ├── internal/         # Логика клиента
│   └── proto/            # Protobuf (копия)
├── client-android/        # Android клиент (Go Mobile)
├── admin-panel/          # Веб админ-панель
└── docs/                 # Документация
```

## Лицензия

Проприетарное ПО. Все права защищены.

## Поддержка

Для получения поддержки обращайтесь к разработчикам через защищённые каналы связи.
